import React from 'react'
import styled from 'styled-components'
import { space, color } from 'styled-system'

const Icon = ({
  size,
  ...props
}) => (
  <svg
    {...props}
    viewBox='0 0 24 24'
    width={size}
    height={size}
    fill='currentcolor'
  >
    <path d='M 12 17 A 2 2 0 0 1 12 21 A 2 2 0 0 1 12 17 M10 3h4v12h-4z' />
  </svg>
)

const PriorityHighIcon = styled(Icon)([], space, color)

PriorityHighIcon.displayName = 'PriorityHighIcon'

PriorityHighIcon.defaultProps = {
  size: 24
}

export default PriorityHighIcon