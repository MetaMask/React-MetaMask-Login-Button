import React from 'react'
import styled from 'styled-components'
import { space, color } from 'styled-system'

const Icon = ({
  size,
  ...props
}) => (
  <svg
    {...props}
    viewBox='0 0 24 24'
    width={size}
    height={size}
    fill='currentcolor'
  >
    <path d='M22 8V2L2 22h16V8z M14 22V10L2 22h12zm6-12v8h2v-8h-2zm0 12h2v-2h-2v2z' />
  </svg>
)

const SignalCellularConnectedNoInternet2BarIcon = styled(Icon)([], space, color)

SignalCellularConnectedNoInternet2BarIcon.displayName = 'SignalCellularConnectedNoInternet2BarIcon'

SignalCellularConnectedNoInternet2BarIcon.defaultProps = {
  size: 24
}

export default SignalCellularConnectedNoInternet2BarIcon