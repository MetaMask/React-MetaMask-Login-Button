import React from 'react'
import styled from 'styled-components'
import { space, color } from 'styled-system'

const Icon = ({
  size,
  ...props
}) => (
  <svg
    {...props}
    viewBox='0 0 24 24'
    width={size}
    height={size}
    fill='currentcolor'
  >
    <path d='M2 22h20V2z' />
  </svg>
)

const SignalCellular4BarIcon = styled(Icon)([], space, color)

SignalCellular4BarIcon.displayName = 'SignalCellular4BarIcon'

SignalCellular4BarIcon.defaultProps = {
  size: 24
}

export default SignalCellular4BarIcon