import React from 'react'
import styled from 'styled-components'
import { space, color } from 'styled-system'

const Icon = ({
  size,
  ...props
}) => (
  <svg
    {...props}
    viewBox='0 0 24 24'
    width={size}
    height={size}
    fill='currentcolor'
  >
    <path d='M22 8V2L2 22h16V8z M20 10v8h2v-8h-2zm-8 12V12L2 22h10zm8 0h2v-2h-2v2z' />
  </svg>
)

const SignalCellularConnectedNoInternet1BarIcon = styled(Icon)([], space, color)

SignalCellularConnectedNoInternet1BarIcon.displayName = 'SignalCellularConnectedNoInternet1BarIcon'

SignalCellularConnectedNoInternet1BarIcon.defaultProps = {
  size: 24
}

export default SignalCellularConnectedNoInternet1BarIcon