import React from 'react'
import styled from 'styled-components'
import { space, color } from 'styled-system'

const Icon = ({
  size,
  ...props
}) => (
  <svg
    {...props}
    viewBox='0 0 24 24'
    width={size}
    height={size}
    fill='currentcolor'
  >
    <path d='M20 18h2v-8h-2v8zm0 4h2v-2h-2v2zM2 22h16V8h4V2L2 22z' />
  </svg>
)

const SignalCellularConnectedNoInternet4BarIcon = styled(Icon)([], space, color)

SignalCellularConnectedNoInternet4BarIcon.displayName = 'SignalCellularConnectedNoInternet4BarIcon'

SignalCellularConnectedNoInternet4BarIcon.defaultProps = {
  size: 24
}

export default SignalCellularConnectedNoInternet4BarIcon