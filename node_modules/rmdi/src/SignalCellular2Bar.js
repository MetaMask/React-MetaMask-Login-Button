import React from 'react'
import styled from 'styled-components'
import { space, color } from 'styled-system'

const Icon = ({
  size,
  ...props
}) => (
  <svg
    {...props}
    viewBox='0 0 24 24'
    width={size}
    height={size}
    fill='currentcolor'
  >
    <path d='M2 22h20V2z M14 10L2 22h12z' />
  </svg>
)

const SignalCellular2BarIcon = styled(Icon)([], space, color)

SignalCellular2BarIcon.displayName = 'SignalCellular2BarIcon'

SignalCellular2BarIcon.defaultProps = {
  size: 24
}

export default SignalCellular2BarIcon