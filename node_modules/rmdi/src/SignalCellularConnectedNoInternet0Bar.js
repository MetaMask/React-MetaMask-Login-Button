import React from 'react'
import styled from 'styled-components'
import { space, color } from 'styled-system'

const Icon = ({
  size,
  ...props
}) => (
  <svg
    {...props}
    viewBox='0 0 24 24'
    width={size}
    height={size}
    fill='currentcolor'
  >
    <path d='M22 8V2L2 22h16V8z M20 22h2v-2h-2v2zm0-12v8h2v-8h-2z' />
  </svg>
)

const SignalCellularConnectedNoInternet0BarIcon = styled(Icon)([], space, color)

SignalCellularConnectedNoInternet0BarIcon.displayName = 'SignalCellularConnectedNoInternet0BarIcon'

SignalCellularConnectedNoInternet0BarIcon.defaultProps = {
  size: 24
}

export default SignalCellularConnectedNoInternet0BarIcon