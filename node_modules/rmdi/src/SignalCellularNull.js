import React from 'react'
import styled from 'styled-components'
import { space, color } from 'styled-system'

const Icon = ({
  size,
  ...props
}) => (
  <svg
    {...props}
    viewBox='0 0 24 24'
    width={size}
    height={size}
    fill='currentcolor'
  >
    <path d='M20 6.83V20H6.83L20 6.83M22 2L2 22h20V2z' />
  </svg>
)

const SignalCellularNullIcon = styled(Icon)([], space, color)

SignalCellularNullIcon.displayName = 'SignalCellularNullIcon'

SignalCellularNullIcon.defaultProps = {
  size: 24
}

export default SignalCellularNullIcon