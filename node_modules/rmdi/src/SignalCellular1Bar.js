import React from 'react'
import styled from 'styled-components'
import { space, color } from 'styled-system'

const Icon = ({
  size,
  ...props
}) => (
  <svg
    {...props}
    viewBox='0 0 24 24'
    width={size}
    height={size}
    fill='currentcolor'
  >
    <path d='M2 22h20V2z M12 12L2 22h10z' />
  </svg>
)

const SignalCellular1BarIcon = styled(Icon)([], space, color)

SignalCellular1BarIcon.displayName = 'SignalCellular1BarIcon'

SignalCellular1BarIcon.defaultProps = {
  size: 24
}

export default SignalCellular1BarIcon